import {Grid, Typography} from "@mui/material"

const Header = () => <>
    <Grid container>
        <Grid item>
            <Typography variant="h4" sx={{ paddingTop: "1vh", paddingLeft: "1vw" }}>Your Logo</Typography>
        </Grid>
    </Grid>
</>

export default Header